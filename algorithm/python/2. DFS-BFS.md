# 1. 그래프 탐색의 기초

- 그래프는 정점(Vertex)과 간선(Edge)으로 구성된 자료구조

## 인접 리스트

- 각 정점에 연결된 정점들을 리스트로 표현
- 메모리 효율적, 간선 추가/삭제 용이

```python
graph = [[] for _ in range(3)]

graph[0].append(1)
graph[0].append(2)    
graph[1].append(2)
graph[2].append(0)
graph[2].append(1)

print(graph)  # [[1, 2], [2], [0, 1]]
```

```python
from collections import defaultdict

# 1. 원본 데이터 (간선 리스트)
edges = [("Seoul", "Busan"), ("Seoul", "Incheon"), ("Busan", "Daegu")]

# 2. 딕셔너리 기반 그래프 생성
graph = defaultdict(list)

for start, end in edges:
    graph[start].append(end)
    # 만약 무방향(양방향) 그래프라면 아래 줄 주석 해제
    # graph[end].append(start)
```

# 2. DFS (Depth-First Search)

- 깊이 우선 탐색
- 스택 자료구조를 활용하여 구현 (재귀 함수 사용 가능)
- 한 경로를 끝까지 탐색한 후, 다른 경로로 이동
- 주로 경로 탐색, 사이클 검출 등에 사용

```python
def dfs(graph, v, visited):
    # 현재 노드 방문 처리
    visited[v] = True
    print(v, end=' ')
    
    # 현재 노드와 연결된 다른 노드를 재귀적으로 방문
    for i in graph[v]:
        if not visited[i]:
            dfs(graph, i, visited)

# 각 노드가 방문된 정보를 리스트로 표현 (0번 노드는 사용 안 함)
visited = [False] * 9
# dfs(graph, 시작노드, visited)
```

# 3. BFS (Breadth-First Search)

- 너비 우선 탐색
- 큐 자료구조를 활용하여 구현
- 인접한 노드를 먼저 탐색한 후, 그 다음 깊이로 이동
- 최단 경로 탐색 등에 사용

```python
from collections import deque

def bfs(graph, start, visited):
queue = deque([start])
# 현재 노드 방문 처리
visited[start] = True

    # 큐가 빌 때까지 반복
    while queue:
        # 큐에서 하나의 원소를 뽑아 출력
        v = queue.popleft()
        print(v, end=' ')
        
        # 아직 방문하지 않은 인접 원소들을 큐에 삽입
        for i in graph[v]:
            if not visited[i]:
                queue.append(i)
                visited[i] = True
```

4. 2차원 리스트에서의 탐색
```python
# 방향 벡터 (상, 하, 좌, 우)
dx = [-1, 1, 0, 0]
dy = [0, 0, -1, 1]

def solution(x, y):
    # 맵의 범위를 벗어나면 즉시 종료
    if x <= -1 or x >= n or y <= -1 or y >= m:
        return False
    # 현재 노드를 아직 방문하지 않았다면
    if graph[x][y] == 0:
        graph[x][y] = 1 # 방문 처리
        # 상, 하, 좌, 우 재귀적으로 호출 (DFS 예시)
        solution(x - 1, y)
        solution(x + 1, y)
        solution(x, y - 1)
        solution(x, y + 1)
        return True
    return False
```