# SQL 활용

## 서브쿼리

- 메인 쿼리문 안에 다른 쿼리문
- IN(목록에 포함), ANY(하나라도 만족), ALL(모든 조건 만족), EXISTS(존재 여부)

### 단일행 서브쿼리

- 서브쿼리가 하나의 값만 반환

### 다중열 서브쿼리

- 서브쿼리가 두 개 이상의 열을 반환

### 다중행 서브쿼리

- 서브쿼리가 여러 행을 반환

### 스칼라 서브쿼리

- SELECT 문에서 서브쿼리가 하나의 컬럼

### 인라인 뷰

- FROM 절에서 임시 테이블처럼 활용
- 뷰는 실제 데이터 생성 x, 임시 목록
- 뷰 장점 : 보안성, 독립성, 편의성

### 상호연관 서브쿼리

- 메인 쿼리의 각 행 마다 서브 쿼리 실행

## 집합 연산자

<img width="676" height="198" alt="image" src="https://github.com/user-attachments/assets/c4e282de-f5a1-403d-82ce-a0ef5d43ff12" />

## 그룹함수

- GROUP BY 절에서 여러 행을 하나의 결과 값으로 요약하는 함수
- 집계함수(COUNT, SUM, AVG, MIN, MAX) 외 ROLLUP, CUBE, GROUPING SET 존재

### ROULLUP

ROLLUP(col1, col2) -> (col1,col2) -> col1 -> 전체 행 순으로 그룹화

- 컬럼 순서 변경 시 결과 달라짐

### CUBE

CUBE(col1, col2) -> (col1,col2) -> col1 -> col2 -> 전체 행 순으로 그룹화

- 조합 가능한 전체 경우를 그룹화, 순서 상관 없음

### GROUPING SETS

- 그룹화할 대상을 지정 가능
- 지정에 따라 ROLLUP이나 CUBE와 같은 값

## 윈도우 함수

- GROUP BY랑 달리, 그룹화하지 않고 각 행에 대하여 계산된 값만 반환
- OVER 키워드와 함께 사용, OVER 안에서 집계함수 사용 불가
- SELECT 단계에서 실행, SELECT, ORDER BY에서만 사용 가능

## 순위 함수

<img width="743" height="123" alt="image" src="https://github.com/user-attachments/assets/3fbe3bb7-b78a-4311-8d57-7e0ad52e574a" />

## 집계 함수

<img width="762" height="655" alt="image" src="https://github.com/user-attachments/assets/b03ea566-2ec7-48ca-b55d-3c55f3d67c80" />

- GROUP BY 없이 OVER 사용
- PARTITION BY col : col을 기준으로 합쳐져서 계산

## 행 간의 값 참조 함수

<img width="778" height="205" alt="image" src="https://github.com/user-attachments/assets/d0698b4a-57fb-4c11-bb1a-613d7eabe09d" />

- 윈도우 함수는 현재 행까지만 제한, LAST_VALUE는 현재 행인 자기 자신 값을 표현, 범위 지정 키워드 사용해야함

## 범위 지정 키워드

<img width="751" height="349" alt="image" src="https://github.com/user-attachments/assets/94771c3a-c511-4f49-9ca6-6f54c81cc9f3" />

## 비율함수

<img width="756" height="554" alt="image" src="https://github.com/user-attachments/assets/3b152531-5fd7-4a15-9485-906593f20e23" />

## Top N 쿼리

- 상위 N개 데이터 추출
  TOP : SQL Server
  LIMIT : MySQL, PostgreSQL
  FETCH FIRST : DB2, Oracle
  ROWNU< : Oracle

- LIMIT, FETCH, TOP 은 ORDER BY 다음 실행
- ORDER BY 필수
- 동일 순위 표시 WITH TIES
- ROWNUM 사용 시 =, > 연산자 사용 불가, < or <= 사용 가능

## 계층형 질의

- 데이터가 계층 구조를 가지고 있을 때, 계층 구조를 탐색하기 위해 사용

<img width="761" height="418" alt="image" src="https://github.com/user-attachments/assets/b0f016ac-1ddb-4882-9ca5-9e81223cd59a" />

<img width="762" height="135" alt="image" src="https://github.com/user-attachments/assets/d13dfccf-dddf-4366-8bad-69299d8c3968" />

## SELF JOIN

- 계층 쿼리를 대신
- 같은 테이블에 대하여 조인 수행

## PIVOT 절

<img width="640" height="191" alt="image" src="https://github.com/user-attachments/assets/8a4c3bbf-aecd-4490-b75c-1783795324e7" />

- 행 데이터를 열로 변환

## UNPIVOT 절

- 열 데이터를 행으로 변환

## 정규표현식

<img width="759" height="407" alt="image" src="https://github.com/user-attachments/assets/368c0909-d5e1-4a0d-af83-0aacc266a107" />

<img width="600" height="486" alt="image" src="https://github.com/user-attachments/assets/28756f8c-0923-4076-a682-d47300035e5e" />

## DML (데이터 조작 언어)

### INSERT

- 넣고자 하는 값들을 순서대로 입력
    - INSERT INTO 학생 VALUES(5,'길동',23,1)
- 명시해서 추가, 명시되지 않은 컬럼은 NULL
    - INSERT INTO 학생 (학번, 이름, 학년) VALUES(5,'길동',23)

### UPDATE

- SET : 변경하고자 하는 값 지정
    - UPDATE 학생 SET 나이 = 20 WHERE 이름 = '선영'

### DELETE

- DDL TRUNCATE도 테이블의 데이터 삭제하나, ROLLBACK 불가

### MERGE

- MERGE INTO A USING B
    - B 테이블을 이용하여 A 테이블에 합병
- WHEN MATCHED THEN
    - ON 조건을 만족하면 수행

## TCL

- ACID
- COMMIT, ROLLBACK, SAVEPOINT
- SAVEPOINT를 설정하면 ROLLBACK은 SAVEPOINT 지점까지만 가능 

### 자동커밋

1. Oracle

- DDL - 무조건 자동커밋
- DML - ON/OFF에 따라 롤백 가능
- 기본으로 자동 커밋 모드 OFF

2. SQL Server

- 자동 커밋 모드 ON/OFF 에 따라 동작
- 기본으로 자동 커밋 모드 ON

## DDL

- 스키마 정의

### CREATE

<img width="604" height="191" alt="image" src="https://github.com/user-attachments/assets/a017bb02-6376-4ffd-8d44-af1ab190549c" />

### ALTER

- ADD
- MODIFY
- DROP COLUMN
- RENAME COLUMN

### DROP

- 참조 걸려있는 경우 삭제 불가
- CASCADE CONSTRAINTS
    - 참조하는 모든 FK 제약 조건도 함께 삭제

### TRUNCATE

- 테이블은 유지, 데이터만 삭제
- ROLLBACK 불가

## DCL

### GRANT
- GRANT 권한 ON 테이블명 TO 유저명
- 권한 부여

### REVOKE

- 권한 회수

### 권한 부여 옵션

<img width="802" height="376" alt="image" src="https://github.com/user-attachments/assets/e0afca0f-acaa-4959-81d3-ab4548cf921e" />

#### 출처 https://www.youtube.com/watch?v=cMQqWsjYyoc